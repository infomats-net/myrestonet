
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Represents a user in the system.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["super_admin", "restaurant_admin", "staff"] },
        "restaurantId": { "type": "string", "description": "The tenant ID this user belongs to (null for super_admin)." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "role"]
    },
    "Restaurant": {
      "title": "Restaurant",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "adminUserId": { "type": "string" },
        "name": { "type": "string" },
        "address": { "type": "string" },
        "city": { "type": "string" },
        "state": { "type": "string" },
        "country": { "type": "string" },
        "baseCurrency": { "type": "string" },
        "baseLanguage": { "type": "string" },
        "subscriptionStatus": { "type": "string", "enum": ["active", "suspended", "trial"] },
        "paymentSettings": {
          "type": "object",
          "properties": {
            "paypal": {
              "type": "object",
              "properties": {
                "enabled": { "type": "boolean" },
                "clientId": { "type": "string" }
              }
            },
            "cod": {
              "type": "object",
              "properties": {
                "enabled": { "type": "boolean" }
              }
            }
          }
        },
        "deliverySettings": {
          "type": "object",
          "properties": {
            "deliveryCharge": { "type": "number" },
            "freeDeliveryAbove": { "type": "number" },
            "deliveryEnabled": { "type": "boolean" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "adminUserId"]
    },
    "Menu": {
      "title": "Menu",
      "description": "A container for a set of dishes (e.g., Main Menu, Drinks).",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "description": { "type": "string" },
        "isAvailable": { "type": "boolean" }
      },
      "required": ["id", "name"]
    },
    "MenuItem": {
      "title": "MenuItem",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "price": { "type": "number" },
        "category": { "type": "string" },
        "isAvailable": { "type": "boolean" }
      }
    },
    "Order": {
      "title": "Order",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "customerId": { "type": "string" },
        "status": { "type": "string" },
        "subtotal": { "type": "number" },
        "deliveryCharge": { "type": "number" },
        "totalAmount": { "type": "number" },
        "paymentMethod": { "type": "string", "enum": ["paypal", "cod"] },
        "paymentStatus": { "type": "string" },
        "items": { "type": "array" },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/backend/entities/UserProfile" },
      "description": "Global users collection for role-based access control."
    },
    "/restaurants/{restaurantId}": {
      "schema": { "$ref": "#/backend/entities/Restaurant" },
      "description": "Tenant root document."
    },
    "/restaurants/{restaurantId}/menus/{menuId}": {
      "schema": { "$ref": "#/backend/entities/Menu" },
      "description": "Tenant-specific menu containers."
    },
    "/restaurants/{restaurantId}/menus/{menuId}/items/{itemId}": {
      "schema": { "$ref": "#/backend/entities/MenuItem" },
      "description": "Dishes within a specific menu."
    },
    "/restaurants/{restaurantId}/orders/{orderId}": {
      "schema": { "$ref": "#/backend/entities/Order" },
      "description": "Tenant-specific orders subcollection."
    }
  }
}
